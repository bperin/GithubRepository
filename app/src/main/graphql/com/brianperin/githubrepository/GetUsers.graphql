query GetUsers($query: String!, $offset: Int!) {
    search(type: USER, query: $query, first: $offset) {
        edges {
            node {
                ... on User {
                    id
                    name
                    login
                    avatarUrl
                    repositories {
                        totalCount
                    }
                }
            }
        }
    }
}
